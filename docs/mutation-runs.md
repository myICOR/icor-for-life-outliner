# Mutation runs

Recorded 2026-09-06 for 0.1.0 by `npm run mutate` (`test/mutate.mjs`). Each row breaks one guard in a copy of the source and lists the fixture cases that went red against it. A row with zero red cases fails the run; the release workflow runs this after the gate.

| Mutation | What was broken | Cases that went red |
| --- | --- | --- |
| `fold-reapply` | the fold re-apply after the replacement | 5: enter: Enter mid-line on a folded item keeps the children folded under the head; indent: Tab keeps the moved item's own fold; move: Move up keeps the folds of the moved item and of its neighbour; move: Move down keeps the folds of the moved item and of its neighbour; outdent: Shift-Tab keeps the moved item's fold |
| `indent-chain` | the indent fallback chain (unit only) | 5: indent: Tab takes the indent step from the previous sibling's first child; indent: Tab prefers the sibling's own step over the step used elsewhere in the list; indent: Tab takes the indent step from the item itself when the sibling has no children; indent: Tab prefers the item's own step over the step used elsewhere in the list; indent: Tab takes the indent step from elsewhere in the list before the editor's unit |
| `indent-chain-sibling` | the first link of the chain (the sibling's children) | 1: indent: Tab prefers the sibling's own step over the step used elsewhere in the list |
| `indent-chain-own` | the second link of the chain (the item's own step) | 1: indent: Tab prefers the item's own step over the step used elsewhere in the list |
| `checkbox-carry` | the task box carried onto the new item | 2: enter: Enter carries a task box over, unchecked; enter: Enter carries a task box onto a new first child |
| `code-fence-bail` | the code-fence bail in the node classifier | 1: enter: Enter inside a fenced code block passes to the editor |
| `single-cursor-guard` | the single-cursor guard | 2: guards: Two cursors pass every key to the editor; guards: Two cursors pass Backspace to the editor |
| `non-list-node-check` | the non-list-node check before any operation | 4: enter: Enter inside a fenced code block passes to the editor; guards: A bullet-shaped line inside frontmatter is not a list; guards: A bullet-shaped line inside a table is not a list; guards: A list inside a callout is left to the editor |
| `merge-safety` | the merge safety rule (Backspace and Delete) | 3: backspace: Backspace between two items with content swallows the key; backspace: Backspace at the content start of a task item swallows the key; delete: Delete between two items with content swallows the key |
| `mixed-indent-refusal` | the refusal of mixed tab and space indentation | 2: guards: Move up refuses a list that mixes tabs and spaces; indent: Tab refuses a list that mixes tabs and spaces |
| `one-replace` | the diff (every changed line replaced one by one) | 38: enter: Enter at the end of an item adds a sibling below it; enter: Enter in the middle of a line moves the tail to the new item; enter: Enter at the end of an item with children makes the new item the first child; enter: Enter mid-line on an item with children hands the children to the new item; enter: Enter at the end of a folded item adds a sibling after the folded subtree; enter: Enter mid-line on a folded item keeps the children folded under the head; enter: Enter carries a task box over, unchecked; enter: Enter carries a task box onto a new first child; enter: Enter renumbers an ordered list; enter: Enter makes a numbered first child and renumbers the old one; enter: Enter on a notes line splits it and moves the later notes along; enter: Enter at the end of the last notes line adds a sibling; enter: Enter at the end of the bullet line moves the notes to the new item; enter: Enter keeps the sibling's bullet style; enter: Enter at the end of a tab-indented child adds a sibling with the same indent; guards: A list under a heading starts at its first bullet; indent: Tab moves the item and its children under the previous sibling; indent: Tab renumbers both ordered groups; indent: Tab keeps the moved item's own fold; indent: Tab carries the item's notes lines; indent: Tab on a notes line indents the item that owns it; move: Move up swaps with the previous sibling and carries the subtree; move: Move up from the first position joins the previous parent as its last child; move: Move down swaps with the next sibling and carries the subtree; move: Move down from the last position joins the next parent as its first child; move: Move up keeps the folds of the moved item and of its neighbour; move: Move down keeps the folds of the moved item and of its neighbour; move: Move up into a folded uncle unfolds it; move: Move up renumbers an ordered list; move: Move up into the previous parent takes that parent's indent step; move: Move down keeps the cursor on its character; move: Move up carries the notes lines; move: Move down from a first child past the parent's siblings; outdent: Shift-Tab moves the item and its children to right after the parent; outdent: Shift-Tab keeps the moved item's fold; outdent: Shift-Tab renumbers both ordered groups; outdent: Shift-Tab in a tab list keeps the child one tab under the item; outdent: Shift-Tab carries the notes lines one level out |

134 cases per mutation, 11 mutations, 0 hollow.
